{
  "name": "academic-reference-checker",
  "version": "1.0.0",
  "description": "A standalone tool to verify academic references.",
  "scripts": {
    "test": "echo "Error: no test specified" && exit 1"
  }
}